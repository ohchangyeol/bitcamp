Dos는 Naming convention(네이밍컴벤션).

시간있을때 소팅 알고리즘 검색.

상속은 일반화, 구체화 통합이라 간단 생각.

관계에선 올라가면 일반화. 내려가면 구체적.

객체지향 기본원리 
리월월드를 표현하는 방법론
리얼월드에 표현하는할수있는것 객체

그것을 세분화 시키는것 추상화
추상화 연관 관계.

field에 int cc; 라고 하면 int 가 0으로 초기화값을 갖고있음

class path - 설정하지않으면 . 으로 디폴트값으로 지정. 해당하는 class가 해당 위치에 있는 instance class 를 찾아간다.

 . 하나는 current Directory
.. 두개는 Parent Directory

return은 void를 생략 값을 전달받음. 전달받은 값의 return data type이 같아야 한다. return 은 무조건 하나만 와야한다.

void 는 return을 받지않음.

public은 공개.

private = 비공개.
	method로 return값을 받아 접근이 가능하다.




